# UiPlan Integration Notes

This project can represent integrations in UiPlan bundles through:

- `spec.md` architecture and constraints sections
- `plan.md` integration/workflow mapping
- `tasks.md` verification tasks and evidence paths

Keep each integration entry explicit: system name, boundary, and evidence.
