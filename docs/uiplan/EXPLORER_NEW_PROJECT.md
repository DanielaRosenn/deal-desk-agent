# Explorer In A New Project

To add UiPlan Explorer support in a new project:

1. Add a UiPlan bundle (`spec.md`, `plan.md`, `tasks.md`).
2. Optionally add `.uiplan/explorer.yaml`.
3. Open the project in UiPlan Studio web UI.
