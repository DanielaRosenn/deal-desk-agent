# UiPlan Studio Guide

UiPlan Studio combines:

- API: bundle discovery, graph endpoints, CopilotKit compatibility endpoints
- Web UI: planning graph and UiPlan canvas

Use it to inspect `spec.md`, `plan.md`, and `tasks.md` visually.
